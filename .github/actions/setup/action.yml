name: Setup
description: Install Node 24 with npm dependency caching and run npm ci (repo must already be checked out)

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 24
        cache: npm
    - run: npm ci
      shell: bash
